apply plugin: 'com.android.application'

android {
    compileSdkVersion 24
    buildToolsVersion "24.0.2"

    defaultConfig {
        applicationId "io.straas.android.sdk.messaging.demo"
        minSdkVersion 10
        targetSdkVersion 24
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    compile "io.straas.android.sdk:straas-messaging-ui:${STRAAS_SDK_VERSION}"
}
